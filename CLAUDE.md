# 저장소 작업 지침

> 이 문서는 `AGENTS.md`(Codex 등이 읽는 지침서)와 **내용이 동일**하다.
> 한쪽을 수정하면 반드시 다른 쪽도 같이 수정해 동기화 상태를 유지한다.

## 커밋 메시지 규칙
- 코드 변경이 포함된 커밋은 **반드시 한글 커밋 메시지**를 사용한다.
- 커밋 제목(첫 줄)은 짧고 명확하게 작성한다.
- 커밋 본문을 추가할 경우 본문도 한글로 작성한다.
- 여러 변경이 있을 때는 변경 목적이 드러나도록 작성한다.

## 커밋 메시지 예시
- `지연 대기 로직에서 음수 sleep 예외 방지 수정`
- `이미지 탐색 중 중단 처리 안정화`

## 코드 수정 후 리뷰 요청 규칙

액터(코드를 수정하는 에이전트)와 리뷰어(리뷰하는 에이전트)는 **리뷰 파일 문서**를 통해 소통한다.

### 대원칙: 액터는 수정까지 끝낸 뒤에 리뷰를 맡긴다
- 액터는 문제를 **발견하는 데서 멈추지 않는다.** 발견 → 수정 → 검증 → 리뷰 요청까지가 한 번의 작업이다.
- "점검해줘", "버그 있는지 봐줘", "이슈 확인해줘" 같은 요청도 마찬가지다.
  문제를 찾았으면 **그 자리에서 고친 뒤** 리뷰 파일을 만든다.
  발견 목록만 보고하고 "수정할까요?"라고 되묻지 않는다.
- 예외적으로 수정하지 않아도 되는 경우는 아래뿐이다. 이때는 그 사유를 리뷰 파일에 적는다.
  - 사용자가 "고치지 말고 점검만" 이라고 명시한 경우
  - 수정 방향이 갈려 사용자의 결정이 필요한 경우 (→ 결정이 필요한 부분만 남기고 **나머지는 전부 수정**한다)
  - 수정 범위가 사용자가 요청한 작업 범위를 명백히 벗어나는 경우
- 리뷰 파일의 `## 리뷰 결과` 섹션은 **리뷰어가 채울 빈 칸**이다. 액터가 자기 발견 사항을 여기에 적지 않는다.
  액터가 찾아서 고친 내용은 `## 발견 및 수정 내역`에 적는다.

### 1) 액터: 수정 완료 후 리뷰 파일 생성
- **코드 수정과 검증을 모두 마친 뒤** `reviews/REVIEW-YYYYMMDD-<주제>.md` 파일을 생성한다.
  - 예: `reviews/REVIEW-20260813-그룹탭-상태갱신.md`
  - 같은 날 같은 주제로 재작업하면 새 파일을 만들지 말고 기존 파일에 라운드를 추가한다.
- 파일에는 아래 템플릿을 채워 넣는다.

```markdown
# 리뷰 요청: <주제>

- 작성일: YYYY-MM-DD
- 대상 브랜치/커밋: <브랜치명 또는 커밋 해시>
- 변경 파일: `main.py`, `macro_logic.py` 등

## 발견 및 수정 내역 (액터 작성)
| # | 문제 (파일:라인) | 원인 | 수정 내용 | 검증 방법 |
|---|---|---|---|---|
| 1 | | | | |

## 수정하지 않은 항목
- (있다면 사유와 함께. 없으면 `없음`)

## 중점 확인 요청
- (액터가 특히 불안하다고 느끼는 지점)

---

## 리뷰 결과 (리뷰어 작성)

### 블로커
- [ ] (파일:라인) 문제 설명 / 재현 조건 / 기대 동작

### 비블로커(참고)
- (선택 사항, 간단히)

---

## 조치 내역 (액터 작성)
- [ ] (블로커 항목별 수정 내용 / 수정 불필요 시 사유)

---

## 리뷰어에게 전달할 멘트
> (아래 인용문을 그대로 복사해 리뷰어에게 전달한다)
>
> `reviews/REVIEW-YYYYMMDD-<주제>.md` 파일을 열어 리뷰해 주세요.
> **블로커 이슈 위주로 봐주세요.** — 동작이 깨지거나 예외가 나는 문제,
> 데이터가 유실되는 문제, 빌드/실행이 안 되는 문제가 우선입니다.
> 스타일·네이밍·취향 지적은 제외해 주세요.
>
> 확인 순서:
> 1. `## 발견 및 수정 내역`의 수정이 실제로 문제를 해결했는지
> 2. 그 수정이 새로운 문제를 만들지 않았는지
> 3. `## 중점 확인 요청`에 적힌 항목
>
> 리뷰 결과는 대화창이 아니라 **이 파일의 `## 리뷰 결과` 섹션**에 적어 주세요.
> 각 항목은 `파일:라인 → 문제 → 재현 조건 → 기대 동작` 형식으로 부탁드립니다.
> 블로커가 없으면 `### 블로커` 아래에 `없음`이라고 적어 주세요.
> 코드는 직접 고치지 말아 주세요. 수정은 제가 합니다.

---

## 액터에게 전달할 멘트 (리뷰어 작성)
> (리뷰어가 리뷰를 마친 뒤 이 자리에 채운다. 그대로 복사해 액터에게 전달한다)
>
> 리뷰를 마쳤습니다. `reviews/REVIEW-YYYYMMDD-<주제>.md`의 `## 리뷰 결과`를 확인해 주세요.
> **블로커 N건 / 비블로커 M건**입니다.
> 블로커 요약: (한 줄씩)
> - B1 (파일:라인) 한 줄 요약
>
> 블로커부터 수정하고, `## 조치 내역`에 항목별로 무엇을 어떻게 고쳤는지 적어 주세요.
> 수정하지 않기로 한 항목은 지우지 말고 사유를 남겨 주세요.
> 수정이 끝나면 다시 리뷰 요청해 주세요.
```

### 2) 액터: 리뷰 요청 멘트 전달
멘트는 **두 군데** 모두에 넣는다.

1. **리뷰 파일 맨 아래** — 위 템플릿의 `## 리뷰어에게 전달할 멘트` 섹션.
   리뷰어가 파일만 열어도 무엇을 어떻게 봐야 하는지 알 수 있어야 한다.
2. **사용자에게 출력하는 응답의 맨 마지막** — 아래 형태로 **반드시** 출력한다.
   응답 중간에 끼워 넣지 말고, 복사해서 바로 쓸 수 있게 마지막에 둔다.

> 수정을 완료하고 리뷰 파일 `reviews/REVIEW-YYYYMMDD-<주제>.md`를 생성했습니다.
> **블로커 이슈 위주로 리뷰해 주세요.** (동작이 깨지거나 예외가 발생하는 문제 우선,
> 스타일·취향 지적은 제외)
> 리뷰 내용은 해당 파일의 `## 리뷰 결과` 섹션에 적어 주시면 그걸 보고 수정하겠습니다.

### 3) 리뷰어: "리뷰해줘"를 받으면

사용자가 새 세션에서 **"리뷰해줘"** 라고만 말해도 아래를 스스로 수행한다.
어떤 파일을 봐야 하는지, 무엇을 리뷰해야 하는지 되묻지 않는다.

**대상 파일 찾기**
1. `reviews/` 폴더의 `REVIEW-*.md` 파일을 확인한다.
2. `## 리뷰 결과`의 `### 블로커`가 비어 있거나 템플릿 그대로(`- [ ] (파일:라인) ...`)인 파일이 대상이다.
3. 대상이 여러 개면 파일명 날짜가 가장 최근인 것을 고르고, 어느 것을 골랐는지 사용자에게 알린다.
4. `reviews/` 폴더가 없거나 리뷰 대기 중인 파일이 없으면 그 사실을 알리고 멈춘다.
   (임의로 다른 것을 리뷰하지 않는다.)

**리뷰 방법**
1. 파일의 `## 발견 및 수정 내역`, `## 수정하지 않은 항목`, `## 중점 확인 요청`을 먼저 읽는다.
2. 거기 적힌 변경 파일을 **실제로 열어 코드를 확인한다.** 리뷰 파일의 설명만 믿지 않는다.
   액터가 "고쳤다"고 적은 것이 실제로 고쳐졌는지는 코드로 확인한다.
3. 확인할 것:
   - 그 수정이 실제로 문제를 해결했는가
   - 그 수정이 **새로운 문제를 만들지 않았는가** (회귀)
   - `## 중점 확인 요청`에 적힌 항목
   - 수정 과정에서 놓친 같은 유형의 다른 문제
4. 가능하면 직접 실행·측정해서 확인한다. 추측으로 지적하지 않는다.

**결과 기재**
- 리뷰 결과는 대화창이 아니라 **리뷰 파일의 `## 리뷰 결과` 섹션**에 적는다.
- **블로커 우선**: 크래시, 예외, 잘못된 동작, 데이터 손실, 빌드 실패, 매크로 중단 실패 등을 먼저 적는다.
- 블로커가 없으면 `### 블로커` 아래에 `없음`이라고 명시한다. 억지로 만들어내지 않는다.
- 각 항목은 `파일:라인 → 문제 → 재현 조건 → 기대 동작` 순으로 구체적으로 적는다.
- 코드는 리뷰어가 직접 고치지 않는다. 수정은 액터가 한다.
- 마지막으로 파일 맨 아래 **`## 액터에게 전달할 멘트`를 채운다.**
- 사용자에게 출력하는 응답의 맨 마지막에도 그 멘트를 그대로 넣는다.

### 4) 액터: "리뷰봐줘"를 받으면

사용자가 **"리뷰봐줘"**, "리뷰 반영해줘", "리뷰 확인해줘" 라고만 말해도 아래를 스스로 수행한다.
어떤 파일을 봐야 하는지 되묻지 않는다.

**대상 파일 찾기**
1. `reviews/` 폴더의 `REVIEW-*.md` 파일을 확인한다.
2. `## 리뷰 결과`에 리뷰어가 채운 내용이 있고, `## 조치 내역`이 비어 있거나
   미완료 항목(`- [ ]`)이 남아 있는 파일이 대상이다.
3. 여러 개면 파일명 날짜가 가장 최근인 것을 고르고, 어느 것을 골랐는지 알린다.
4. 대상이 없으면 그 사실을 알리고 멈춘다.
   - `## 리뷰 결과`가 템플릿 그대로면 **"아직 리뷰가 오지 않았다"** 고 알린다. 임의로 자문자답하지 않는다.
   - `### 블로커`가 `없음`이고 조치 내역도 끝나 있으면 **완료 상태**임을 알린다.

**반영 방법**
1. **블로커부터** 순서대로 수정하고, 그다음 비블로커를 처리한다.
2. 지적된 코드를 직접 열어 확인한 뒤 고친다. 리뷰 내용을 그대로 믿고 고치지 않는다.
3. 리뷰 지적이 틀렸다고 판단되면 임의로 무시하지 말고, **근거와 함께** `## 조치 내역`에 적는다.
4. 수정 후 실제로 실행·측정해 검증한다.
5. 처리한 항목은 `## 리뷰 결과`의 체크박스를 `[x]`로 바꾸고,
   `## 조치 내역`에 항목별로 무엇을 어떻게 고쳤는지 적는다.
6. 수정하지 않기로 한 항목은 사유를 남긴다. **임의로 삭제하지 않는다.**
7. 이번 수정이 새로운 문제를 만들지 않았는지 스스로 확인한다.

**다시 리뷰 요청**
- 반영이 끝나면 새 파일을 만들지 말고 **같은 파일에 라운드를 추가**한다.
  - `## 리뷰 결과 (2차)` 빈 섹션을 만들고, `## 리뷰어에게 전달할 멘트`를 2차용으로 갱신한다.
  - 이전 라운드의 내용은 그대로 남겨 이력을 유지한다.
- 사용자 응답 맨 마지막에 `### 2)`의 리뷰 요청 멘트를 다시 출력한다.
- 리뷰어가 `### 블로커 = 없음`을 낼 때까지 2~4단계를 반복한다.

## 실행 파일 요청 처리 규칙
- 사용자가 실행 파일 생성을 요청하면 `Pucro.spec` 기준으로 빌드한다.
- 빌드가 완료되면 `dist\Pucro.exe`를 바탕화면 `Pucro.exe`에 **덮어쓰기까지 수행**한다.
- 빌드 시 `tkinter` 누락 방지를 위해 가능하면 영문 경로(예: `C:\Users\skarb`)에서 명령을 실행한다.
